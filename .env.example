# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# ============================================================================
# REQUIRED FOR FRONTEND (Safe to expose in client bundle)
# ============================================================================

# Public Supabase URL
VITE_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anonymous/Public Key (respects Row Level Security)
VITE_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# OPTIONAL - SERVER-SIDE ONLY (NEVER commit, NEVER use in frontend!)
# ============================================================================

# Service Role Key for backend testing with curl
# WARNING: This key bypasses RLS. Server-side tests only!
# DO NOT reference this in any frontend code
# DO NOT commit .env.local or any file containing this key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Get credentials from Supabase Dashboard  Settings  API
# 3. Replace VITE_PUBLIC_SUPABASE_URL with your Project URL
# 4. Replace VITE_PUBLIC_SUPABASE_ANON_KEY with your anon public key
# 5. (Optional) Add SERVICE_ROLE_KEY only for local curl testing
# 6. Restart dev server: npm run dev
# 7. NEVER commit .env.local to git (already in .gitignore)
