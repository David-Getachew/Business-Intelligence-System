# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Public Supabase URL (safe to expose in frontend)
VITE_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public Supabase Anon Key (safe to expose in frontend, RLS policies protect data)
VITE_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ DO NOT add SUPABASE_SERVICE_ROLE_KEY here!
# Service role key should ONLY be used in serverless functions, never in frontend code
# Set it as an environment variable in your deployment platform (Vercel, Netlify, etc.)

